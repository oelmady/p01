#include "Passenger.h"
#include "PassengerQueue.h"

int main(){
    /*
    Passenger a(1,2,3);
    Passenger b(2,2,4);
    Passenger c(3,1,3);
    PassengerQueue q;
    q.enqueue(a);
    q.enqueue(b);
    q.enqueue(c);
    q.print(cout);
    */
    return 0;
}